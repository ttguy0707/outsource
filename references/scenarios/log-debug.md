# 场景 · 日志 / 报错排查

**适用**：按 pattern grep 指定日志、给时间窗，只报命中条目，不臆测根因（根因判断归主线）。

## 委派提示词（整段发给目标 session，已内嵌产出/报告格式）
```
# 角色
你是日志分析领域专家。本次任务：在指定日志里按 pattern 把命中原文捞出来如实回报，只收集证据、不下根因结论。

# 背景（帮你理解在找什么）
- 日志路径：<…>；时间窗：<起-止>
- 要找：<pattern/关键字/错误码>；现象：<…>

# 步骤（按序，每步留真实输出作证据）
1. grep：<grep -nE "<pattern>" <日志路径>，带时间过滤>　目的：捞命中行
2. 统计频次、首次/末次出现时间

# 铁律
- 只读，不改任何文件。
- **只报命中原文（时间+整行），逐字贴**，不概括、不下"应该是 xx 导致"这类结论。
- 命中为空就如实说"未命中"，不脑补。
- 卡点如实写。

# 产出（你按下面格式回传）
## 日志排查报告
1. 命中条数：__   时间窗：<起-止>
2. 命中条目（时间 + 原文）：
   ```
   <粘贴命中行>
   ```
3. 频次 / 首次 / 末次：__
4. 卡点：<无则写"无">
```